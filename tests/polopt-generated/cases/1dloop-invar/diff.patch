--- input.pretty.loop
+++ optimized.loop
@@ -1,5 +1,9 @@
 context(M, N, i);
 
-for i0 in range(M, (N + 1)) {
-  a[i] = b[i];
+if ((M + (-1 * N)) <= 0) {
+  for i0 in range((M / 32), ((N + 32) / 32)) {
+    for i1 in range(max(M, (32 * i0)), min((N + 1), ((32 * i0) + 32))) {
+      a[i] = b[i];
+    }
+  }
 }
