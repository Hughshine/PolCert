--- input.pretty.loop
+++ optimized.loop
@@ -1,9 +1,17 @@
 context(N);
 
-for i0 in range(0, N) {
-  x[i0] = (a[i0] + b[i0]);
+if (1 <= N) {
+  x[0] = (a[0] + b[0]);
+  s = 0;
+  for i1 in range(1, N) {
+    x[i1] = (a[i1] + b[i1]);
+  }
 }
-s = 0;
-for i0 in range(0, N) {
-  s = (s + (x[i0] * 3));
+if (1 <= N) {
+  for i1 in range(0, N) {
+    s = (s + (x[i1] * 3));
+  }
 }
+if (N <= 0) {
+  s = 0;
+}
