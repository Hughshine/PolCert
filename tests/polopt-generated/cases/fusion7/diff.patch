--- input.pretty.loop
+++ optimized.loop
@@ -1,8 +1,12 @@
 context(N, M);
 
-for i0 in range(0, N) {
-  a[i0] = b[i0];
+if (1 <= N) {
+  for i1 in range(0, N) {
+    a[i1] = b[i1];
+  }
 }
-for i0 in range(0, M) {
-  a[i0] = b[i0];
+if (1 <= M) {
+  for i1 in range(0, M) {
+    a[i1] = b[i1];
+  }
 }
