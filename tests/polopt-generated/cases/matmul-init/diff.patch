--- input.pretty.loop
+++ optimized.loop
@@ -1,10 +1,18 @@
 context(N);
 
-for i0 in range(0, N) {
+if (1 <= N) {
   for i1 in range(0, N) {
-    C[i0][i1] = 0;
     for i2 in range(0, N) {
-      C[i0][i1] = (C[i0][i1] + (A[i0][i2] * B[i2][i1]));
+      C[i1][i2] = 0;
     }
   }
 }
+if (1 <= N) {
+  for i1 in range(0, N) {
+    for i2 in range(0, N) {
+      for i3 in range(0, N) {
+        C[i1][i2] = (C[i1][i2] + (A[i1][i3] * B[i3][i2]));
+      }
+    }
+  }
+}
