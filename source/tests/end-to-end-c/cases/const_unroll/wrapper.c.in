#include <stdio.h>

static int A[4];

static int checksum(void) {
  int sum = 0;
  for (int i = 0; i < 4; ++i) {
    sum += A[i] * (i + 1);
  }
  return sum;
}

int main(void) {
  for (int i = 0; i < 4; ++i) {
    A[i] = 0;
  }
  /* POLCERT_KERNEL */
  printf("%d\n", checksum());
  return 0;
}
