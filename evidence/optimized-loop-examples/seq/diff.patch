--- input.pretty.loop
+++ optimized.loop
@@ -1,7 +1,9 @@
 context(N, M);

-for i0 in range(0, N) {
-  for i1 in range(0, M) {
-    s = (s + a[i0][i1]);
+if (1 <= M) {
+  for i0 in range(0, N) {
+    for i1 in range(0, M) {
+      s = (s + a[i0][i1]);
+    }
   }
 }
